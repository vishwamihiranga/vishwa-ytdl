[build]
  command = "npm install"
  publish = "public"

[functions]
  directory = "functions"

[dev]
  functions = "functions"
  port = 3000

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
